import { DashboardContainer } from './index'

/**
 * Demo component showing how to use the dashboard system
 * This can be integrated into the main application routing
 * @returns {JSX.Element}
 */
const DashboardDemo = () => {
  return (
    <div>
      <DashboardContainer />
    </div>
  )
}

export default DashboardDemo